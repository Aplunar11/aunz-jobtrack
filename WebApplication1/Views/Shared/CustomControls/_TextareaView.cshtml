@model JobTrack.Models.FormFieldModel

<div class="form-group">
    <label>@Model.Label</label>
    <textarea class="form-control" 
              rows="3" 
              name="@Model.Name" 
              @(Model.IsReadOnly ? "readonly" : "")>@Model.Value</textarea>
</div>